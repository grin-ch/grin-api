syntax = "proto3";

package grin.api.auth;
option go_package = "grin-api/api/auth";

message AuthReq {
  string app_id = 3;
}
message AuthRsp {
  string access_token = 1;
}

service AuthService {
  rpc Auth(AuthReq) returns (AuthRsp);
}